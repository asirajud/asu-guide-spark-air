# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
build/
next-env.d.ts

# env / secrets — never commit the AIR key
.env
.env.*
!.env.example

# local databases (regenerate with `pnpm db:seed`)
*.db
*.db-shm
*.db-wal

# misc
.DS_Store
*.log
.vercel
.idea/

# the corpus lives with the app (asu-guide/data/); this is the scratch copy
/asu-events.json

# Generated agent pointer files. `next dev` writes AGENTS.md (which we keep)
# plus a one-line pointer file beside it that we do not track.
?LAUDE.md

# asu-tools-api runtime registry state (services.json is the checked-in seed)
/asu-tools-api/registry.json

# build-harness scratch files
.air-sentinel
*.tsbuildinfo
.history/
.air-done/

docs/pitch-deck-glm-5-3.html
docs/pitch-deck-kimi-k2-7-code.html
docs/pitch-deck-qwen3-coder-30b-a3b-instruct.html
